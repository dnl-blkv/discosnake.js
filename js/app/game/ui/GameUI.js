define([
        './DiscoSnakeMenuItem',
        'engine',
        './ScoreBoard'
    ],
    function (
        DiscoSnakeMenuItem,
        engine,
        ScoreBoard
    ) {
        'use strict';

        function GameUI () {

        }

        return GameUI;
    });